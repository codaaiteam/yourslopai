export const metadata = {
  title: 'Guess the Prompt \u2013 Your AI Slop Bores Me',
  description:
    'See an image and guess which prompt created it. Pick the right prompt from 4 choices in this fun AI guessing game.',
};

export default function GuessPromptLayout({ children }) {
  return children;
}
