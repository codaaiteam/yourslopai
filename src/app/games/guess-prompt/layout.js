export const metadata = {
  title: 'Guess the Prompt \u2013 Your AI Slop Bores Me',
  description:
    'See a human drawing and guess which prompt they were trying to draw. Pick the right prompt from 4 choices in this fun guessing game.',
};

export default function GuessPromptLayout({ children }) {
  return children;
}
